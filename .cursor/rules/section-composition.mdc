---
description: Composição de seções full-viewport e práticas de layout do portfólio
globs: "{index.html,assets/css/**/*.css,assets/sass/**/*.scss}"
alwaysApply: false
---

# Composição de seções

## Uma função por seção

Cada seção (`#hero`, `#sobre`, `#experiencia`, …) tem: um título, um subtítulo curto, um bloco principal. Evitar repetir cópia já coberta em outra seção (ex.: anos/cargos em Sobre se Experiência já lista).

## Full-viewport vs fluxo natural

### Classe `full-viewport-section`

Usada em todas as seções principais **para padding/largura compartilhados**. Por padrão: `min-height: auto` (altura = conteúdo).

### Hero — única seção que preenche o viewport

Só `#hero` usa `min-height: 100svh` / `100dvh`:

- Conteúdo compacto + CTA/scroll inferior
- Colunas irmãs com densidade parecida — sem coluna “oca”

```css
/* ✅ Só o Hero força viewport cheio */
@media (min-width: 1024px) {
  #hero.full-viewport-section {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-bottom: var(--scroll-reserve, 4.5rem);
  }
}
```

### Seções magras (fluxo natural)

`#projetos`, `#sobre`, `#experiencia`, `#tecnologias`, `#publicacoes`, `#contato`:

- **Não** forçar `100vh` / `100svh` / `100dvh` na seção
- **Não** esticar painéis com `min-height` baseado em `vh` nem `justify-content: center` para “preencher” vazio
- Em desktop/FHD: escalar tipografia/padding — **não** alongar altura
- Sobre: CSS dedicado em `assets/css/about.css`

## Mobile-first estrutural

1. HTML em ordem de leitura (identidade → texto → detalhes → CTAs)
2. CSS empilha por padrão
3. Só ramificar em 2 colunas quando os blocos tiverem **peso visual similar**

## CSS

- Estilos de seção em `assets/css/` (sem `<style>` inline)
- Preferir tokens/`clamp` a magic numbers
- Dark mode via classes/tokens existentes do projeto
- Novos CSS linkados em `index.html` e, se bundling, no `vite.config.js`

## Checklist visual

- [ ] Sem overflow-x em 320px
- [ ] Foto/meta/CTAs legíveis em todas as larguras
- [ ] Em FHD, tipografia e padding acompanham a resolução (sem forçar 100vh em seções magras)
- [ ] Links externos com `rel="noopener noreferrer"`
